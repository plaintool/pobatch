name: Build Windows
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

permissions:
  contents: write
  issues: read

jobs:
  build:
    runs-on: windows-latest
    env:
      CERT_PFX: ${{ secrets.CERT_PFX }}
      VERSION: ${{ inputs.version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      # Cache installers to avoid re-downloading every run
      - name: Cache Lazarus installer
        id: cache-lazarus
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/lazarus-4.8.exe
          key: lazarus-installer-4.8

      - name: Cache FPC installer
        id: cache-fpc
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/fpc-i386.exe
          key: fpc-installer-3.2.2

      - name: Cache Inno Setup installer
        id: cache-inno
        uses: actions/cache@v5
        with:
          path: ${{ runner.temp }}/innosetup-6.7.3.exe
          key: inno-installer-6.7.3

      - name: Install build dependencies
        shell: pwsh
        run: |
          # Install Lazarus 4.8
          $lazarusInstaller = "$env:RUNNER_TEMP\lazarus-4.8.exe"
          if (-Not (Test-Path $lazarusInstaller)) {
              curl.exe -L --retry 5 --retry-delay 10 --retry-all-errors --connect-timeout 60 --max-time 900 "https://sourceforge.net/projects/lazarus/files/Lazarus%20Windows%2064%20bits/Lazarus%204.8/lazarus-4.8-fpc-3.2.2-win64.exe/download" -o $lazarusInstaller
          }
          Start-Process -FilePath $lazarusInstaller -ArgumentList "/VERYSILENT","/SUPPRESSMSGBOXES","/NORESTART" -Wait

          # Install 32-bit FPC
          $fpcDir = "C:\Lazarus\fpc\3.2.2"
          $fpcInstaller = "$env:RUNNER_TEMP\fpc-i386.exe"
          if (-Not (Test-Path $fpcInstaller)) {
              curl.exe -L --retry 5 --retry-delay 10 --retry-all-errors --connect-timeout 60 --max-time 900 "https://sourceforge.net/projects/freepascal/files/Win32/3.2.2/fpc-3.2.2.i386-win32.exe/download" -o $fpcInstaller
          }
          Start-Process -FilePath $fpcInstaller -ArgumentList "/VERYSILENT","/SUPPRESSMSGBOXES","/NORESTART","/DIR=$fpcDir" -Wait

          $env:Path = "C:\Lazarus\fpc\3.2.2\bin\i386-win32;$env:Path"
          "PATH=$env:Path" | Out-File -FilePath $env:GITHUB_ENV -Append

          # Install Inno Setup 6.7.3
          $innoInstaller = "$env:RUNNER_TEMP\innosetup-6.7.3.exe"
          if (-Not (Test-Path $innoInstaller)) {
              curl.exe -L --retry 5 --retry-delay 10 --retry-all-errors --connect-timeout 60 --max-time 900 "https://github.com/jrsoftware/issrc/releases/download/is-6_7_3/innosetup-6.7.3.exe" -o $innoInstaller
          }
          Start-Process -FilePath $innoInstaller -ArgumentList "/VERYSILENT","/SUPPRESSMSGBOXES","/NORESTART" -Wait

      - name: Copy Inno Setup language files
        shell: pwsh
        run: |
          $src = "$env:GITHUB_WORKSPACE\installer\innolanguages\*"
          $dst = "C:\Program Files (x86)\Inno Setup 6\Languages"
          Copy-Item -Path $src -Destination $dst -Recurse -Force

      - name: Locate 32-bit FPC
        shell: pwsh
        run: |
          $candidates = @(
            'C:\\lazarus\\fpc\\3.2.2\\bin\\i386-win32\\fpc.exe',
            'C:\\Lazarus\\fpc\\3.2.2\\bin\\i386-win32\\fpc.exe'
          )
          $found = $candidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if ($found) {
            "FPC32_PATH=$found" | Out-File -FilePath $env:GITHUB_ENV -Append
          } else {
            Write-Error '32-bit FPC not found under Lazarus. Check Lazarus install.'
          }

      - name: Locate FPC config
        shell: pwsh
        run: |
          $cfgCandidates = @(
            'C:\\lazarus\\fpc\\3.2.2\\bin\\i386-win32\\fpc.cfg',
            'C:\\Lazarus\\fpc\\3.2.2\\bin\\i386-win32\\fpc.cfg',
            'C:\\lazarus\\fpc\\3.2.2\\bin\\i386-win32\\..\\fpc.cfg',
            'C:\\Lazarus\\fpc\\3.2.2\\bin\\i386-win32\\..\\fpc.cfg'
          )
          $cfg = $cfgCandidates | Where-Object { Test-Path $_ } | Select-Object -First 1
          if ($cfg) {
            "FPCCFG=$cfg" | Out-File -FilePath $env:GITHUB_ENV -Append
          } else {
            Write-Error "fpc.cfg not found under Lazarus install."
          }

      - name: Build 64-bit binary
        shell: cmd
        run: build.cmd

      - name: Build 32-bit binary
        shell: cmd
        env:
          PATH: C:\\Lazarus\\fpc\\3.2.2\\bin\\i386-win32;%PATH%
          FPCCFG: ${{ env.FPCCFG }}
          FPCDIR: ${{ env.FPCDIR }}
          FPC: ${{ env.FPC32_PATH }}
        run: build32.cmd

      - name: Build installers (x64)
        shell: cmd
        env:
          BUILD_PORTABLE: 0
        run: installer\\build.cmd x64 %VERSION%

      - name: Build installers (x86 + portable)
        shell: cmd
        env:
          BUILD_PORTABLE: 1
        run: installer\\build.cmd x86 %VERSION%

      - name: Build installer Inno Setup (x86 + x64)
        shell: cmd
        run: installer\buildinno.cmd %VERSION%

      - name: Upload build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: build-windows-artifacts
          path: |
            installer/*.exe
            installer/*.msi
            installer/*.zip
